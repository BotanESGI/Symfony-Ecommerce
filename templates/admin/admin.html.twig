<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin - Backoffice{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-gray-100 text-gray-900">
<div class="flex min-h-screen">
    <aside class="w-64 bg-white shadow-lg p-4">
        <div class="p-4">
            <h2 class="text-xl font-bold text-blue-500">Admin Panel</h2>
        </div>
        <nav class="mt-4">
            <ul class="space-y-6 px-4">
                <li>
                    <a href="{{ path('login') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-tachometer-alt mr-3"></i> Tableau de bord
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_products') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-box mr-3"></i> Produits
                    </a>
                </li>
                <li>
                    <a href="{{ path('login') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-shopping-cart mr-3"></i> Commandes
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_cart') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-shopping-basket mr-3"></i> Panier
                    </a>
                </li>
                <li>
                    <a href="{{ path('login') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-users mr-3"></i> Utilisateurs
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_addresses') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-map-marker-alt mr-3"></i> Adresses
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_categories') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-th-list mr-3"></i> Catégories
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_reviews') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-star mr-3"></i> Avis
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_tags') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-tag mr-3"></i> Tags
                    </a>
                </li>
                <li>
                    <a href="{{ path('home_page') }}" class="flex items-center px-4 py-2 text-gray-700 hover:bg-blue-100 hover:text-blue-700">
                        <i class="fas fa-home mr-3"></i> Retour au site
                    </a>
                </li>
            </ul>
        </nav>
    </aside>

    <main class="flex-1 bg-gray-50 p-6">
        {% for message in app.flashes('success') %}
            <div class="mb-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
                <span class="block sm:inline">{{ message }}</span>
            </div>
        {% endfor %}

        {% for message in app.flashes('error') %}
            <div class="mb-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                <span class="block sm:inline">{{ message }}</span>
            </div>
        {% endfor %}

        {% block body %}
            <h1 class="text-2xl font-bold text-gray-700">Bienvenue sur le panneau d'administration</h1>
            <p class="mt-2 text-gray-600">Utilisez le menu à gauche pour naviguer entre les différentes sections.</p>
        {% endblock %}
    </main>
</div>


{% block javascripts %}{% endblock %}
</body>
</html>
