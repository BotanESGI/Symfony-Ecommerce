{% extends 'base.html.twig' %}

{% block title %}API - Devenez Partenaire{% endblock %}

{% block body %}
    <div class="max-w-6xl mx-auto p-6 bg-white shadow-md rounded-lg mt-6">
        <h1 class="text-3xl font-bold mb-6">API - Devenez Partenaire</h1>
        <p class="text-gray-700 mb-4">Vous êtes une marketplace ou une entreprise souhaitant devenir partenaire ? Découvrez notre API gratuite qui vous permet d'accéder à notre catalogue de produits et de l'intégrer facilement à votre système.</p>

        <h2 class="text-2xl font-semibold mt-8 mb-4">Endpoints Disponibles</h2>

        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-100">
            <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Méthode</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Endpoint</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Description</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Action</th>
            </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
            <tr>
                <td class="px-6 py-4 whitespace-nowrap">GET</td>
                <td class="px-6 py-4 whitespace-nowrap">{{ app.request.getSchemeAndHttpHost() }}/api/products</td>
                <td class="px-6 py-4 whitespace-nowrap">Récupère l'intégralité de notre catalogue de produits.</td>
                <td class="px-6 py-4 whitespace-nowrap">
                    <a href="{{ app.request.getSchemeAndHttpHost() }}/api/products" target="_blank" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-700">Essayer</a>
                </td>
            </tr>
            <tr>
                <td class="px-6 py-4 whitespace-nowrap">GET</td>
                <td class="px-6 py-4 whitespace-nowrap">{{ app.request.getSchemeAndHttpHost() }}/api/products/{id}</td>
                <td class="px-6 py-4 whitespace-nowrap">Obtenez les informations d'un produit spécifique en utilisant son ID.</td>
                <td class="px-6 py-4 whitespace-nowrap">
                    <a href="{{ app.request.getSchemeAndHttpHost() }}/api/products/1" target="_blank" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-700">Essayer</a>
                </td>
            </tr>
            </tbody>
        </table>

        <h2 class="text-2xl font-semibold mt-8 mb-4">Utilisation de notre API</h2>
        <p class="text-gray-700 mb-4">Notre API est gratuite et ne nécessite pas de clé d'API. Les requêtes doivent être envoyées en utilisant les méthodes HTTP appropriées. Nous utilisons le format JSON pour toutes les réponses.</p>
    </div>
{% endblock %}
