{% extends 'base.html.twig' %}

{% block title %}Confirmation de la commande{% endblock %}

{% block body %}
    <div class="container mx-auto mt-8">
        <h1 class="text-2xl font-bold mb-6 text-center">Merci pour votre commande!</h1>
        <div class="mt-6 text-center">
            <a href="{{ path('home_page') }}" class="w-full bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 transition-colors">
                Retour à l'accueil
            </a>
        </div>
    </div>
{% endblock %}
